# Пример переменных для docker-compose (скопируйте в docker/.env при необходимости)

# NWN_WEB_TASK_ROOT задаётся в docker-compose.yml как /data/tasks

# CORS для API (если обращаетесь к API напрямую, не через nginx)
# NWN_WEB_CORS_ORIGINS=https://example.com,https://www.example.com
